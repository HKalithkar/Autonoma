apiVersion: v1
kind: Secret
metadata:
  name: autonoma-secrets
type: Opaque
stringData:
  DATABASE_URL: "REPLACE_ME"
  SERVICE_TOKEN: "REPLACE_ME"
  AUDIT_INGEST_TOKEN: "REPLACE_ME"
  PLUGIN_GATEWAY_TOKEN: "REPLACE_ME"
  GITOPS_WEBHOOK_TOKEN: "REPLACE_ME"
  LLM_API_KEY: "REPLACE_ME"
